# Minimal dependencies for Lambda runtime
# These are the only packages needed for Lambda functions to run

# AWS SDK (Lambda already has boto3, but we include it for consistency)
boto3>=1.34.0

# Data Validation
pydantic>=2.9.0
pydantic-settings>=2.6.0

# AWS Lambda Specific
aws-lambda-powertools>=3.2.0

# Logging
structlog>=24.4.0

# Configuration
python-dotenv>=1.0.0

# Utilities
python-dateutil>=2.9.0
pytz>=2024.2

# JSON handling
orjson>=3.10.0
